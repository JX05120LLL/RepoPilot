services:
  qdrant:
    image: qdrant/qdrant:v1.18.2
    ports:
      # 仅向本机暴露 HTTP API，避免局域网直接访问向量数据。
      - "127.0.0.1:6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data:
